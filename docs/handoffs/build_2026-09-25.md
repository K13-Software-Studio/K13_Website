# Build handoff: beta_v8 "The Workbench" · 2026-09-25

- **Status:** BUILT, verified, on branch `site13_sep25_v1` (cabin worktree), not pushed, not merged. Waiting for Kazim's review on `http://localhost:9130/`.
- **Summary:** The whole of Kazim's ask from 2026-09-24/25 in one branch: the sculpted-film opening (48222ae), the externalised files and tightened CSP (e78cb6d), and the full rebuild (76dd088 plus this commit): two named people instead of one builder, four client doors, work tickets, five playable mechanics from real projects, the craft showroom switch, a dated proof section, the two-person studio block, an honest three-way contact, the hidden layer (find the 13, night shift, blueprint, idle pencil), the studio's motion vocabulary, and every defect from the 2026-09-24 QA baseline.
- **Files:** `index.html`, `css/site.css`, `css/workbench.css`, `css/404.css`, `js/site.js`, `js/intro.js`, `js/workbench.js`, `vercel.json`, `sitemap.xml`, `assets/og/k13-og.png`, `assets/shots/webp/*`, `assets/intro/*`, `docs/BUILD_SPEC_2026-09-25.md`, `docs/copy_2026-09-25.md`, `CLAUDE.md`.
- **Risks:**
  - The Gmail button opens a new tab: browsers that block pop-ups get an honest note instead.
  - "Send it" still hands a `mailto:` to the OS, as before; the two fallbacks exist for visitors without a mail app.
  - The opening film is 1.2 MB and plays once per visit; it is the heaviest thing on the page by design.
  - The workbench needs JavaScript; a `<noscript>` line says so. Nothing else on the page depends on it.
  - Team K13 is deliberately absent (Kazim, 2026-09-25); the "Also cooking" line no longer mentions the sports private equity home.
- **Next:** Kazim reviews → `hm++` (PR, merge, Before & After on the Work, Workbench and Hero sections, production fetch). Then Irina's security pass on the tightened CSP and Olga's independent QA when the account limit allows an agent run.
- **Human gate:** Kazim's review of the whole page, and his word to ship.

## How it was built
The marketing copy came from the Marketing Specialist (`docs/copy_2026-09-25.md`). The page and game engineers were cut off by the account's usage limit at 04:10 (rate limit on the subagent model), so James built the rest directly against the spec; no stage was skipped, only the who.

## Verification (all on the cabin, served with the exact `vercel.json` headers)
| Check | Result |
|---|---|
| axe-core 4.10.2 (wcag2a/aa/21aa/22aa/best-practice), 1440 and 390 | 0 violations: light, dark (night shift), showroom on, reduced motion, after every game was played and all 13 marks found |
| CSP with `script-src 'self' https://cdn.jsdelivr.net` and `style-src 'self' https://fonts.googleapis.com` | 0 violations on index (opening playing and after) and 404 |
| Console / page errors | 0 |
| fitcheck, 11 viewports (320, 360, 375, 390, 430, 768, 844×390, 932×430, 1024, 1440, 1920) | horizontal leak 0 everywhere; tap targets under 24px: 0; keyboard walk: no focused element invisible |
| Page weight | desktop first view 395 KB (fonts 224); full phone scroll 1,006 KB incl. all 13 shots (611 KB images), film excluded; baseline was 3.3 MB |
| Work links | all 13 fetched 2026-09-25, all 200 |
| Copy rules | 0 em dashes in rendered text; "Team K13" 0; "one builder" 0; no purple-leaning hex in any stylesheet |
| Games | each driven by script with mouse and keyboard: scale reads, bet reveals EV and the morning line, pins drop and count, canvas paints (pointer and arrows+space), 13 bulbs light and chase |
| Hidden layer | 13 marks placed (6 page, 7 cards), none inside another control; counter reads "13 of 13"; dialog focus-trapped, Esc closes, "Say hello" stores the subject and the composer uses it; 13 lockup clicks toggle dark and reveal the footer switch; Konami draws 10 section boxes and Esc clears |
| Contact | Gmail URL asserted: subject and body encoded correctly with quotes, angle brackets, ampersands and newlines; copy button only says "Copied" after the clipboard promise resolves; "Send it" verified only through the shared `compose()` (see CLAUDE.md correction) |
| Reduced motion (phone) | opening never appears, no attract loops, games still playable, 0 axe violations |

## Local proof server
```python
# serves the cabin with vercel.json's headers (minus HSTS) and Range support for the film
python3 <scratchpad>/srv.py /Users/k13/git-store/worktrees/K13_Website/site13_sep25_v1   # 127.0.0.1:9130
```
